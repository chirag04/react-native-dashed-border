/**
 * @providesModule DashedBorder
 * @flow
 */

'use strict';

var { requireNativeComponent, } = require('react-native');
var DashedBorder = requireNativeComponent('RNDashedBorder', null);

module.exports = DashedBorder;